spring:
  application:
    name: dubbo-gateway-sample-provider
  cloud:
    nacos:
      config:
        namespace: dev
        prefix: dubbo-gateway-sample-provider
        server-addr: 127.0.0.1:8848
        file-extension: yml        